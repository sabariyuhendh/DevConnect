generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id             String         @id @default(cuid())
  email          String         @unique
  name           String?
  username       String         @unique
  password       String?
  bio            String?
  location       String?
  website        String?
  github         String?
  linkedin       String?
  profilePicture String?
  coverPicture   String?
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt
  posts          Post[]
  comments       Comment[]
  likes          Like[]         @relation("UserLikes")
  sessions       Session[]
  refreshTokens  RefreshToken[]
  followers      Follow[]       @relation("FollowedBy")
  following      Follow[]       @relation("Following")
}


// Implementation removed — Prisma schema to be reimplemented by the user.
